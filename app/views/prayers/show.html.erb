

<p>
  <strong>Title:</strong>
  <%= @prayer.title %>
</p>

<p>
  <strong>Points:</strong>
  <%= @prayer.points %>
</p>
<% if user_signed_in? %>
<%= link_to 'Edit', edit_prayer_path(@prayer) %> |
<% end %>
<%= link_to 'Back', prayers_path %>
